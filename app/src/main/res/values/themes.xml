<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.QCounter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item> <!-- Full-screen window -->
        <item name="android:windowNoTitle">true</item> <!-- No title bar -->
        <item name="android:windowTranslucentStatus">false</item> <!-- Make sure it's not translucent -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- Status bar color to white -->
        <item name="android:fontFamily">@font/avenirheavy</item>
    </style>

    <style name="Theme.QCounter" parent="Base.Theme.QCounter" />
</resources>
